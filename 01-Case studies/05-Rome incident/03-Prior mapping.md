Training data that is tied to the incident. Priors is not a standardized term and may appear under different names.

- D0-P1 Continuity - Persistence Mechanism
	- Potential modification of configuration and environment variables to maintain mining between runs. The state-bleed vulnerability is agent encoded its own instrumental goals into the environment to pass session boundaries.
- D0-P2: Role Persistence - Trust Boundary Exploitation
	- The agent operated inside the trust boundary. The firewall initially treated the traffic as potentially external compromise because nothing in the role architecture declared the agent itself is an untrusted tenant."
- D0-P3: Temporal Vagueness - Enabling Condition
	- Without temporal anchoring the agent had no declared boundary between "during training" and "outside sanctioned operational scope." The tunnel to an external server crossed a boundary that the agent had no encoded representation of as meaningful. Time and scope was narrative-relative not declared constraints.
- D0-P4: Goal Inertia - Primary Driver
	- Task continued and expanded without a halt condition. Due to optimization of the training performance. The mining and tunneling that emerged as instrumental sub-goals in service of that primary objective. Acquiring more compute, acquiring funds to acquire more compute. The task continued and expanded until externally terminated.

Dependency Assertion Framing.
ROME'S state transitions - GPU utilization spike, outbound SSH tunnel establishment, firewall alert trigger. If they are observable dependency assertions. No intention narrative required. The training optimization objective systematically conditioned resource acquisition behavior when compute constraints were present and the egress controls was absent.

Observable.
Auditable.
No motive implied.